# ORCA Backend — Session Summary

**Date:** 2026-08-23
**Goal:** Upgrade ORCA from a rule-based pipeline (keyword matching + static thresholds) into a genuine multi-agent **LLM** system — on a **zero-cost** stack.

---

## 1. What was done

### Phase 0 — Codebase exploration & first run
- Read all source files (`models.py`, `orchestrator.py`, `agents/*`, `main.py`, `test_run.py`).
- Installed deps, ran `python test_run.py` (rule-based mode) and started `uvicorn main:app --port 8000`.

### Phase 1 — LLM upgrade (original spec was Anthropic/Claude)
User had no budget for Anthropic API keys → provider switched to **Groq's free tier**
(30 RPM / ~1,000–14,400 RPD per model, no credit card). Architecture unchanged from the original plan:

```
raw query
   │
   ▼
PlanningAgent ── LLM call #1 (forced tool call → guaranteed JSON:
   │             intent / location_name / time_window / agents_needed / why)
   ▼
OceanStateAgent ── deterministic seeded simulation (UNTOUCHED)
   │              + LLM call #2 → reasoning_note ("report to a colleague")
   ▼
HazardAgent ── fixed numeric thresholds (UNTOUCHED)
   │          + LLM call #3 → reasoning_note explaining the verdict
   ▼
SynthesisAgent ── LLM call #4 → ONE coherent user-facing answer
   │              + flags cross-agent contradictions in `conflicts`
   ▼
OrchestratorResponse (same dataclass contract + new fields)
```

Key design rules enforced in prompts:
- LLMs reason **on top of** the deterministic data layer; they may never invent numbers.
- Structured outputs use **forced tool calling** (`tool_choice` pinned), not string parsing.
- Every LLM step produces a normal `AgentTrace` entry with real `duration_ms`.
- **Graceful degradation:** missing key / API failure after retry-once → falls back to the
  original rule-based path, trace shows `[mode=rules]` / `[llm_unavailable]`. Demo never crashes.

### Files created
| File | Purpose |
|---|---|
| `llm_client.py` | Single shared wrapper: OpenAI-compatible SDK → Groq endpoint, dotenv key loading (`GROQ_API_KEY`), `complete()` + `complete_structured()` (forced tool call), retry-once wrapper, `LLMUnavailableError` |
| `agents/synthesis_agent.py` | Final-answer agent: consumes query + ocean reading + risk assessment + both reasoning_notes; returns answer + conflicts list; deterministic fallback template preserved |
| `.env.example` | `GROQ_API_KEY`, optional `LLM_BASE_URL`, `LLM_MODEL` |

### Files modified
| File | Change |
|---|---|
| `models.py` | Added `reasoning_note: str = ""` to `OceanStateReading` and `RiskAssessment`; added `conflicts: list[str]` to `OrchestratorResponse`. Existing fields untouched. |
| `agents/ocean_state_agent.py` | Simulated-data logic byte-identical; added `_generate_reasoning_note()` LLM layer on top |
| `agents/hazard_agent.py` | Threshold logic untouched; added `_generate_reasoning_note()` LLM layer |
| `orchestrator.py` | Planning Agent now LLM-based via `PLANNING_TOOL_SCHEMA` (intent enum, location enum from `KNOWN_LOCATIONS`, time_window enum); old keyword methods kept as fallback; dispatch + SynthesisAgent wiring |
| `test_run.py` | Prints each agent's `reasoning_note` (visible agent conversation) + flagged conflicts |
| `requirements.txt` | Added `openai>=1.40`, `python-dotenv>=1.0` |

---

## 2. Issues hit & fixes

| Issue | Cause | Fix |
|---|---|---|
| `localhost refused to connect` | Server was never started | Started uvicorn as background process |
| All LLM calls silently fell back (`[llm_unavailable]`) despite valid key | Groq **retired `llama-3.3-70b-versatile`** → 404 `model_not_found`; retries were swallowed by graceful-degradation design | Listed live models via `/models` endpoint; switched to **`openai/gpt-oss-120b`** in `llm_client.py`, `.env.example`, and the user's `.env` |
| Trace showed `Dispatching to .` in fallback mode | Empty `agents_needed` join | Display-chain fallback logic in orchestrator |
| Mojibake (`�`) in terminal output | PowerShell console UTF-8 rendering only | Not a data issue — API JSON is clean UTF-8 |

---

## 3. Current state

- ✅ Server running at `http://localhost:8000` (docs UI at `/docs`)
- ✅ Live LLM mode verified end-to-end (~1–4 s/query = 4 Groq calls):
  - Trace shows `[mode=llm]`
  - Ocean/Hazard notes written conversationally ("Hey Hazard Agent, ...")
  - Synthesis cites only provided numbers + honestly discloses simulated data
- ✅ Fallback path verified (works identically with no key)

## 4. Honest limitation (told to user)

**Replies are NOT hardcoded** — every response is freshly generated by the LLM.
**But the underlying numbers are simulated**: seeded deterministically per
`(location, time_window)`, tagged `"simulated"` in `evidence_sources`, until real
data sources are wired in.

## 5. Phase 2 — Live production data (Open-Meteo)

**Goal executed:** replace simulated ocean-state data with real, keyless, no-registration
live feeds. No placeholder mode; live path is now the default.

### Live proof (raw API check before coding)
Open-Meteo queried for Ratnagiri (16.9902, 73.3120) returned real hourly data,
timezone auto-resolved to Asia/Kolkata: waves 1.70–1.92 m, SST 28.4–28.7 °C,
gusts up to 43.6 km/h.

### Changes made

| File | Change |
|---|---|
| `models.py` | Added `DataSource.LIVE = "live"`; added `field_sources: dict` to `OceanStateReading` — per-field provenance (`"live"` / `"simulated"`), no blanket tagging |
| `agents/ocean_state_agent.py` | **Full rewrite of the data path.** `_fetch_live()` implemented for real against Open-Meteo Marine (wave height, SST, swell) + Weather (wind speed/gusts requested in km/h). Timezone-correct hour picking via `utc_offset_seconds` + local target time (`today` → current hour, `tomorrow` → 12:00, `tomorrow_morning` → 09:00); exact-hour match else nearest hour; null-safe nearest-value lookup; unit defensively verified from `hourly.units`. `USE_LIVE_DATA = True` is now the default. Old seeded simulator renamed `_fetch_simulated_fallback()` and used ONLY as an error fallback — failures are logged with reason via `logging` and the trace result_summary explicitly says "Open-Meteo unavailable (...) used cached/simulated fallback". Chlorophyll/tide simulators renamed `_simulate_chlorophyll()` / `_simulate_tide()`, tagged `SIMULATED` per-field while sst/wave/wind are tagged `LIVE`. Confidence raised to 0.85 on live readings (model forecast, not buoy). |
| `data_connectors/isro_sources.py` **(new)** | MOSDAC / INCOIS / IMD connector stubs built against publicly documented request patterns — explicitly **NOT ACTIVATED**: every `fetch()` raises, every class documents "# TODO: requires registered API key", nothing in the running system imports or calls them. |
| `.env.example` | Added commented slots: `MOSDAC_API_KEY=`, `INCOIS_API_KEY=`, `IMD_API_KEY=` with registration URLs |
| `test_run.py` | Prints FIELD-BY-FIELD DATA PROVENANCE block ([LIVE]/[SIM ] per field); UTF-8 stdout reconfigure for Windows cp1252 consoles |

### Verified end-to-end with live data (2026-08-23, ~17:43 IST)
| Location | Window | Wave | SST | Wind | Gusts | Verdict |
|---|---|---|---|---|---|---|
| Ratnagiri | tomorrow morning | **1.74 m** LIVE | **28.4 °C** LIVE | 6.0 km/h LIVE | 21.2 km/h LIVE | CAUTION |
| Kochi | today | **1.50 m** LIVE | **28.5 °C** LIVE | 5.9 km/h LIVE | 15.8 km/h LIVE | SAFE |
| Visakhapatnam | today | **1.76 m** LIVE | **28.3 °C** LIVE | 15.3 km/h LIVE | 33.1 km/h LIVE | CAUTION |

Ratnagiri values cross-checked against the raw Open-Meteo JSON fetched before any code
was written — identical (same hourly index selected).

### Still honest about limitations
- **chlorophyll + tide remain simulated** (no free keyless source exists); individually
  tagged in `field_sources`, disclosed by the agents' reasoning notes.
- MOSDAC/INCOIS/IMD are never described as live anywhere until credentials exist.

## 6. Phase 2.1 — Response quality fixes (found during user's live /docs testing)

User ran a real query through the Swagger UI and pasted the full JSON back.
Verdict: data/verdict/provenance all correct, but **two LLM-output flaws** were spotted:

| # | Flaw in the response | Root cause | Fix |
|---|---|---|---|
| 1 | `ocean_state.reasoning_note` ended mid-sentence (*"...gusting to"*) | `gpt-oss` is a reasoning model — hidden thinking tokens consume the same completion budget, so some calls silently truncated at low `max_tokens` | (a) `llm_client.complete()` now treats `finish_reason == "length"` as a failure → triggers the retry-once path; (b) note budgets raised 220→600 tokens in both agents |
| 2 | Final answer cited *"a tide of 2.05 m"* as if measured — tide is SIMULATED | Synthesis prompt never saw `field_sources`, so it blended simulated numbers into prose undistinguished | SynthesisAgent now injects the per-field provenance table into its prompt + new rule 5: *"If you cite a simulated field's number, explicitly say that value is estimated/simulated, not measured."* |

Diagnostic used to confirm root cause 1: direct API call showed
`finish_reason: stop, completion_tokens: 153` on a short prompt — proving truncation only
occurs when reasoning runs long, i.e. stochastic, hence retry is the right handler.

### Post-fix verification (live query, Kochi)
- Answer now states: *"The tide level (2.05 m) and chlorophyll concentration (1.81 mg/m³)
  are simulated estimates, but they do not affect the safety assessment."*
- Both agent notes end with complete sentences.

Files touched this phase: `llm_client.py`, `agents/ocean_state_agent.py`,
`agents/hazard_agent.py`, `agents/synthesis_agent.py`.

Cosmetic note: `â¯Â°C`-style garbling seen in PowerShell output is console codepage
rendering only; the FastAPI JSON itself is clean UTF-8 (verify in /docs).

## 7. Current status & remaining ideas

Done this session:
- ✅ Real multi-agent LLM pipeline (Groq `openai/gpt-oss-120b`, free tier) on top of untouched deterministic logic
- ✅ Live production ocean data via Open-Meteo (default path), per-field provenance honesty
- ✅ Graceful degradation everywhere (no key → rules; Open-Meteo down → logged simulated fallback)
- ✅ Full explainability trace incl. per-agent notes, durations, conflicts field

Remaining ideas (not started):
1. Add next specialist agents (PFZ, Tide, Route) per README roadmap.
2. Optional: swap Planning Agent enum-constrained locations for a geocoder.
3. Automated test suite (`test_suite.py`) covering time-window picking, routing edge cases,
   degraded-path injection, and rate-limit behavior.
4. Multi-turn session memory (currently each query is independent).

## 8. Phase 3 — LangGraph orchestration (2026-08-24)

**Goal executed:** replace the hardcoded sequential dispatch in
`Orchestrator.handle_query()` with a real **LangGraph StateGraph**, per the
target topology in `orca_final_architecture.py` (planning → conditional
routing → specialist chain → synthesis), while keeping every existing agent,
the Groq client, live-data logic, and the `OrchestratorResponse` contract
byte-compatible — `main.py` and `test_run.py` unchanged.

### Graph topology now running

```
START -> planning --[intent==safety_check]--> ocean_state -> hazard -> synthesis -> END
              \---[otherwise]-----------> unsupported -------------------------------^
```

| Concept from architecture file | Implementation |
|---|---|
| Shared graph state (`ORCAState` TypedDict) | `ORCAGraphState` carrying raw query, plan, context, agent results, response |
| Add-only merge channel (`Annotated[..., operator.add]`) | `traces` channel — every node appends its `AgentTrace`; ready for parallel fan-out later |
| Conditional edges / orchestrator-selected routing | `_route_after_plan()` on the Planning Agent's parsed intent |
| Separate synthesis + response nodes | Single `synthesis` node (SynthesisAgent already emits the final user-facing answer) |

### Files changed

| File | Change |
|---|---|
| `orchestrator.py` | Rewritten: `StateGraph` built once in `__init__` (`_build_graph`), node methods (`_node_planning/_node_ocean_state/_node_hazard/_node_synthesis/_node_unsupported`) as thin adapters over shared step helpers; identical sequential flow preserved as `_handle_query_sequential()`. Planning logic (`_plan`, forced tool schema, rule fallbacks) untouched. |
| `requirements.txt` | Added `langgraph>=0.2` (installed: langgraph 1.2.11) |

Agents (`ocean_state_agent.py`, `hazard_agent.py`, `synthesis_agent.py`),
`llm_client.py`, `models.py`, `main.py`, `test_run.py`: **untouched**.

### Graceful degradation extended one layer down

New rule: if the `langgraph` package itself is missing (import error),
`handle_query()` transparently runs the exact previous direct-call sequence.
Consistent with the project ethos: the demo never hard-crashes.

### Verified end-to-end (all three execution paths)

1. **LLM mode** (`test_run.py`, 3 queries): trace `[mode=llm]`, LIVE Open-Meteo
   fields, full 4-entry trace through graph nodes — identical output shape to
   pre-migration runs.
2. **Unsupported intent**: "What is the capital of France?" → planner returns
   `unknown` → conditional edge routes to `unsupported` node → roadmap answer.
3. **Degraded mode** (bogus API key): `[mode=rules]` keyword planning +
   `[llm_unavailable]` notes, live data still fetched, verdict computed.
4. **API smoke test**: uvicorn up, `/health` ok, `/query` returned
   `status=CAUTION, trace_entries=4`.

### Why this matters for the roadmap

Adding PFZ / Geospatial / Tide agents is now purely additive: one node method
+ one `add_node` + one edge (or an entry in the planning schema's agent list,
mirroring the architecture file's `Send`-based fan-out). The add-only traces
channel means parallel specialists can be introduced without restructuring.

## 9. Phase 4 — Full multi-agent LangGraph pipeline (2026-08-24, afternoon)

**Goal executed:** cover the PS documentation's conversational agent set —
Language/Intent (#1), Planner (#2), PFZ (#3), Ocean-State (#4), Hazard/Alert
(#5), Geospatial Reasoning (#6), Synthesis (#7), Response (#9) — all talking
to each other through the LangGraph graph, per ORCA_Project_Documentation.pdf
Section 7. Explainability Trace (#8) remains the cross-cutting add-only trace
channel; Proactive Monitor (#10) is still pending (async loop, next phase).

### New agents & data

| File | What it does |
|---|---|
| `agents/language_agent.py` **(new)** | Detects query language (11 languages incl. Marathi/Hindi/Tamil/...), normalizes to English for planning; Unicode-script heuristic fallback when no LLM. |
| `agents/pfz_agent.py` **(new)** | Nearest-PFZ lookup. Bhuvan WMS GetFeatureInfo coded but disabled (`USE_LIVE_BHUVAN_PFZ=False`; endpoint probe returned 404 — needs account verification). Zone derived deterministically per location+window, tagged `SIMULATED` in result + field_sources; LLM note discloses it. |
| `agents/geospatial_agent.py` **(new)** | Pure-Python point-in-polygon + segment-distance geofence vs treaty-digitized IMBL lines & MPA polygons (15 km approach buffer); sampled safe-route planner with perpendicular detours around flagged zones (A*/GEBCO = documented TODO). |
| `data/marine_boundaries.geojson` **(new)** | Tier-2 static boundaries: India–Sri Lanka IMBL (1974 Palk Strait + 1976 Gulf of Mannar segments), India–Pakistan IMBL (Sir Creek seaward approx.), Gulf of Mannar MPA, Malvan Marine Sanctuary. Explicitly labeled simplified / NOT FOR NAVIGATION. |
| `agents/response_agent.py` **(new)** | PS #9 split out of synthesis: composes the final answer in the user's detected language (verified fluent Marathi), reasoning woven in, ≤130 words, hard rules against inventing places/values. |
| `agents/synthesis_agent.py` **(rewritten)** | Now pure reconciliation: verdict + confidence + conflicts_resolved + key_points consumed by Response Agent; handles absent specialists (Optional params). |

### Orchestrator rewired

- Planning schema v2: intents now `{safety_check, pfz_lookup, route_plan,
  geofence_check, hazard_alerts, unknown}`; rule-based keyword router extended
  to match.
- `INTENT_DEFAULT_AGENTS` maps each intent to its warranted specialist set;
  planner's `agents_needed` intersects with it; explicit device GPS or
  destination ALWAYS forces the Geospatial Agent.
- `QueryContext` grew `language`, optional `device_gps`, optional
  `destination`; `OrchestratorResponse` grew `language`, `pfz`, `geofence`,
  `route` (main.py `/query` accepts them; JSON-safe serialization verified).
- **Fan-out lesson learned:** native per-agent graph branches double-fired the
  synthesis/response join when branches ended in different supersteps (hazard
  finishes ~3 s after ocean). Tried list-routing AND the Send API — both
  re-fire. Solution: single `specialists` dispatch node running selected
  agents concurrently via `ThreadPoolExecutor` (ocean→hazard chained inside
  one worker; PFZ and Geospatial truly parallel alongside). Exactly one
  synthesis pass, real wall-clock parallelism preserved. Documented in code.
- Per-specialist failures are caught, logged, and skipped — a dead PFZ agent
  no longer kills a safety query.

### Bugs found & fixed during verification

1. `_step_plan` read plan from state before writing it → pass plan explicitly.
2. Geospatial note computed but never stored on `GeofenceStatus.reasoning_note`.
3. ResponseAgent trace missing from response.trace (append after assemble).
4. Hallucinated harbours in a route answer → added "never introduce locations
   not provided" + 130-word cap to Response prompt (re-verified clean).

### Verified end-to-end

- All 5 sample intents + a Marathi query (`mr` detected; answer composed in
  Marathi citing live wave/wind numbers with simulated fields disclosed).
- Geofence test point 3.5 km off the simplified Palk Strait IMBL segment →
  correctly FLAGGED; Odisha offshore point → clear, 78.4 km route planned.
- Degraded mode (bogus key): script-heuristic language detection, keyword
  intent routing (`pfz_lookup`), all specialists run, template answers.
- Server smoke test (in-process uvicorn): full payload round-trip incl.
  pfz/geofence/route objects, 5-entry trace, GPS-forced geospatial dispatch.

### Remaining (PS gaps, next phases)

1. Proactive Monitor Agent (#10): async timer loop + alert push (+ Twilio SMS).
2. Bhuvan PFZ live: verify WMS endpoint/auth, flip `USE_LIVE_BHUVAN_PFZ`.
3. IMD API credentials for cyclone/lightning/fishermen-warning (stubs ready).
4. Multi-turn context memory ("what about the day after?").
5. Voice STT/TTS + map UI + Agent Orchestration Console (frontend scope).

---

## 10. Phase 5 — P0 completion + full P1 sweep + frontend wiring (2026-08-25/26)

**Goal executed:** close every P0 gap from AGENTS.md, then all P1 items,
then wire the ORCA UI to the live backend. The system is now a complete
10/10-component implementation, runnable with ONE command.

### Phase 5.0 — Chlorophyll connector fixed (was the long-standing "network block")

Root cause found: `erdMWchla8day` was RETIRED by NOAA -- griddap answered
HTTP 400 (server was always reachable; earlier sessions misdiagnosed it).

| Change | Detail |
|---|---|
| `data_connectors/chlorophyll.py` rewritten | Primary: `nesdisNPPN20S3ASCIDINEOFDaily` (VIIRS+OLCI DINEOF L4, global 9 km daily); fallback `noaacwNPPN20S3ASCIDINEOF2kmDaily` |
| Query syntax fix | This grid has one altitude level -> ERDDAP rejects strides: must be `chlor_a[(last)][(0)][(lat)][(lon)]`; variable renamed `chlorophyll` -> `chlor_a` |
| Null-pixel hardening | Offshore cells can be land-masked: retries a small cross of nearby wet cells before failing |

Verified LIVE: Kochi 11.31, Ratnagiri 11.63 mg/m3 (real monsoon-upwelling
values). Every ocean-state field is now genuine live data.

### Phase 5.1 — P0 items closed

1. **Proactive Monitor Agent (#10)** -- `agents/proactive_monitor.py`:
   asyncio timer loop (default 15 min) re-polling the SAME
   OceanState/Hazard/Geospatial agents per registered user; signature-based
   dedup (alerts fire only on new/escalating hazards or boundary approach);
   composes alerts in the user's language via LLM (verified fluent Marathi).
2. **Alert delivery** -- `alerts.py`: in-memory alert bus + pub-sub;
   SSE stream endpoint (`GET /alerts/stream/{user_id}`) + polling
   (`GET /alerts/{user_id}`); Twilio SMS for life-safety alerts via plain
   REST (no new dependency, honestly disabled without credentials);
   user registry (`POST /users/register`, position heartbeat, list/delete).
3. **Multi-turn session memory** -- `sessions.py`: TTL store keyed by
   session_id carrying location/time-window/language/GPS/destination;
   planner receives conversation-memory line and resolves 'same place';
   verified over HTTP ("What about the day after?" reuses Ratnagiri).
4. **Lightning data** -- no new feed needed: IMD CAP already carries
   thunderstorm/lightning events. New generic matcher in `imd_cap.py`
   (`match_alerts_by_terms`, `alerts_touching_location`) serves cyclone,
   marine-bulletin AND lightning checks from ONE fetched alert list.

### Phase 5.2 — All P1 items

| # | Item | Implementation | Proof |
|---|---|---|---|
| 6 | IMD fishermen/marine bulletins surfaced | non-cyclone marine CAP alerts hit-tested vs location -> HazardFlag UNSAFE when covering | reasoning lines in verdicts |
| 7 | Hazard-aware routing plumbing | two-phase dispatch: ocean->hazard + pfz parallel, THEN geospatial receiving hazard flag labels + exceedance windows | route avoids flagged zones |
| 8 | Bathymetry + A* | `data_connectors/bathymetry.py` on ETOPO180 (NOAA CoastWatch ERDDAP, keyless, ONE bbox CSV request per route); pure-Python A* grid (~4 km cells, <250 km legs) blocking land/shallow cells (<10 m draft) | 67.9 km route around 390 blocked cells, min depth -49 m |
| 10 | Exact hours + exceedance windows | planner schema v3 adds `target_hour` (0-23); OceanStateAgent computes threshold-crossing intervals over next 48 h from the hourly series; Response cites them | "tomorrow at 10 am" hits exact hour; answer cites gusts >45 km/h 07:00-19:00 peaking 64.8 |
| 11 | Trend/analytics intent | new `agents/trend_agent.py`: MUR monthly SST + DINEOF daily chlorophyll history (index-based ERDDAP slices), calendar-month means, linear trends + Pearson r, LLM narration grounded in stats | real 7-month analysis for Ratnagiri |
| 12 | Region scan | PFZ ranks top-3 alternate thermal-front cells (>=20 km separation); response-level `avoid_zones` merges geofence hits + hazard flags | alternates listed per query |
| 13 | Viz payloads | `GET /viz/{session}` GeoJSON FeatureCollection (query point, PFZ+alternates, route LineString, CAP hazard polygons) + `/viz/{session}/series` hourly charts | round-tripped over HTTP |
| 14 | Tide highs/lows | `tide.predict_highs_lows()` evaluates the harmonic fit at 10-min steps, local extrema + daily range, LOCAL wall-clock times | "low 05:00 (0.16 m), high 12:10 (0.94 m)" cited |

New intents in planner v3: `trend_analysis`, `zone_scan`; rule-based router
extended to match.

### Phase 5.3 — API surface additions (`main.py`)

- lifespan hook starts/stops the Proactive Monitor with the app.
- `/users/*`, `/alerts/*` (poll + SSE), `/viz/{session}[/series]`,
  `DELETE /sessions/{id}`; every `/query` response now carries `session_id`
  and is stored for viz.

### Phase 5.4 — Frontend wired to the live backend

| Change | File(s) |
|---|---|
| Real API service replacing mocks: POST /query with session ids, real trace -> activity steps, SAFE/CAUTION/UNSAFE verdict callouts, graceful mock fallback | `ORCA UI/src/services/orcaApiService.ts` |
| Backend probe w/ auto-retry every 15 s while offline, loud LIVE / CONNECTING / DEMO MODE header chip (click = reconnect), toast on simulated responses, proactive SSE alerts injected as chat messages | `store/appState.ts`, `components/layout/Header.ts` |
| ONE-COMMAND startup: vite plugin spawns `python -m uvicorn main:app --port 8000` (skips if :8000 already healthy, tree-kills on exit, logs prefixed `[orca-be]`), plus `/api` proxy for future same-origin deploys | `vite.config.ts`, package.json scripts |

Verified end-to-end over HTTP: session-memory follow-ups, registration ->
immediate Marathi UNSAFE alert (gusts + IMBL proximity), viz payloads.

### Bugs found & fixed during verification

1. naive-vs-aware datetime comparison in exceedance scan (Open-Meteo local
   strings are naive).
2. `(risk, trace)` tuple unpacking in phase-2 dispatch.
3. ERDDAP series queries must constrain EVERY dimension (altitude `[(0)]`
   on the DINEOF grid).
4. 'NaN' strings in ERDDAP CSVs filtered before statistics.
5. ETOPO snapping: it is a REGULAR 1-arc-minute lat/lon grid -- no cosine
   scaling on longitude steps.
6. Windows + `--reload`: bare `uvicorn.exe` wrapper re-imports itself in the
   reloader child -> use `python -m uvicorn ...` (or rely on the vite plugin,
   which does exactly that).
7. Test GPS point that plotted "offshore Odisha" was inland (north of an
   E-W coast) -- ETOPO correctly refused it; wet corridor verified first.

### Repo

Pushed to private GitHub: `FarhanFarooqui122/orca-sih26176`.
`.env` is git-ignored (only `.env.example` tracked). Teammates: create own
`.env` with a free Groq key, `pip install -r requirements.txt`,
`npm install`, then `npm run dev` from `ORCA UI`.

### Still remaining

1. Bhuvan WMS live verification / INCOIS official PFZ advisory (credentials).
2. Voice STT/TTS endpoints (PS Sec. 17) -- only major PS item left.
3. Storage layer (Redis/PostGIS/TTL cache), auth/CORS hardening, Dockerfile.
4. Numeric confidence scoring, data-tier labels, configurable thresholds.

---

## 11. Remaining Work — TODO (post Phase 5)

Prioritised for SIH evaluation impact; top four first.

### Priority build order

1. [x] **Voice STT/TTS** -- `POST /query/voice` (audio -> Groq Whisper STT ->
       existing graph; browser TTS speaks the answer in the detected language)
       + real MediaRecorder mic capture in the UI Composer. DONE.
2. [x] **Map & charts in the UI** -- Leaflet operations map (query point, PFZ
       zones + ring, route LineString, IMBL/MPA flags, CAP hazard polygons)
       and 48-h wave/gust SVG charts with thresholds + exceedance spans +
       tide chips in the agent panel, fed by `/viz/{session}` and
       `/viz/{session}/series`. DONE.
3. [x] **Docs refresh** -- root README fully rewritten (features, API,
       status/roadmap); backend README updated to current pipeline
       (Discussion Agent, panel/direct modes). DONE.
4. [x] **Docker** -- ORCA_Backend + ORCA UI Dockerfiles, nginx SPA config,
       root `docker-compose.yml` (`docker compose up --build`). DONE.

### Engineering polish (P2)

5. [x] Storage layer: storage.py TTLStore -- Redis when REDIS_URL is set,
       in-process TTL dict otherwise; sessions.py rewired onto it, /query
       carries a 120 s response cache, /health reports the backend. DONE.
6. [x] Auth/CORS hardening: CORS_ORIGINS allow-list (default * for dev),
       optional ORCA_API_KEY (X-API-Key on all routes except /health),
       allow-listed methods/headers + README data-privacy note. DONE.
7. [x] Configurable safety thresholds per vessel class:
       small_fishing_boat | mechanized_trawler | coastal_cargo via
       request `vessel_class`, plus ORCA_THRESHOLD_OVERRIDES JSON; ocean
       exceedance windows use the same profile as the verdict. DONE.
8. [x] Numeric confidence: response.confidence_score in [0,1]
       (0.55*provenance live-fraction + 0.30*verdict confidence +
       0.15*cross-agent agreement) and evidence_tiers labelling every
       source Tier 1/2/3. DONE.
9. [x] Latency trim: external-data path fixed earlier (~90 s -> ~12 s);
       LLM chain trimmed -- ASCII fast-path skips the language-detection
       call on English queries, retry delay 1.5->0.8 s, discussion token
       budget tightened, 120 s identical-request cache absorbs UI retries.
       Remaining chain is inherently sequential (plan -> specialists ->
       debate -> reconcile -> respond). DONE (further fusion = roadmap).

### Blocked externally (code ready, waiting on credentials/access)

10. [ ] Bhuvan WMS live activation / INCOIS official PFZ advisory
        (endpoint verification + ISRO account).
11. [ ] api.imd.gov.in key (secondary fallback only -- keyless CAP feed
        already covers cyclone/marine/lightning checks).
12. [ ] Cyclone cone-of-uncertainty / wind-warning GeoJSON overlays once
        the IMD key exists (connector functions already coded).
13. [ ] Twilio SMS live-fire test (TRAI DLT registration path or trial
        verified numbers; sending code complete).

### Housekeeping

14. [x] Deleted `orca_final_architecture.py` (stale sketch; its ideas --
         SMS + monitor loop -- live properly in alerts.py /
         agents/proactive_monitor.py since Phase 5). DONE.

---

## 12. Session 2026-08-27 — UI polish, concise answers, voice live-STT (chat transcript)

**Context:** User pulled `457834e`, ran UI+backend locally, found MOSDAC Download API works (5× `E06OCML4AC_20260325-30_25km` .nc in `mdapi/mosdac_downloads`), Leaflet missing fixed via `npm install`, then flagged two UX gaps: (a) generic mock answers for `hi` with 2-step "Agent Execution Steps" pushing the verdict off-screen, (b) verbose answers even when backend LIVE, (c) right Agent Panel trace always expanded.

**What was done this session:**

| Area | Change | File:line | Commit |
|---|---|---|---|
| DESIGN.md alignment | Teal `#00626a/#0e7c86` primary, flat `1px #D1D5DB` tiers, `8px` grid, `4/6/8/16/24` radii, `none` shadows (light+dark) | `ORCA UI/src/styles/variables.css:1` | `422879d` |
| Header/blur | Removed `rgba+blur(4px)` header/backdrop → solid `var(--bg-surface)` | `layout.css:337` | `68ab602` |
| Focus | `2px #0e7c86 + 4px white offset` | `global.css:41` | `830b5d1` |
| Typography | `label-caps 0.08em`, `data-mono 14px`, `display-data 48px`, `headline-lg 32px`, hero `22→32px` | `components.css:4` | `62e0d76` |
| Mock fallback | `general` no longer hallucinates "AI workflow" essay; `hi` → ORCA onboarding, else demo banner | `services/mockAIService.ts:136` | `5642e22` |
| Message layout | Answer first (`ai-msg-body`), trace hidden from bubble (kept only in side panel) | `components/chat/MessageItem.ts:94` | `c4fad4b` |
| Concise answers | `ResponseAgent` prompt `150→70 words`, 2-4 sentences, verdict first | `agents/response_agent.py:180` | `2edecfc` |
| Backend crash | Missing `DataSource` import → `NameError` on `orchestrator._tier_for` (500 on every /query) | `orchestrator.py:74` | `f0ad9f9` |
| Agent Panel | Trace collapsed by default, auto-expands only while `isStreaming`; toggle persists | `components/agents/AgentActivity.ts:18`, `store/appState.ts:55` | `a1b139e` |
| Voice live-STT | Google-style interim `SpeechRecognition` → text streams into composer live; transcript preserved across re-render on stop; Whisper used only as fallback when no live text | `components/chat/Composer.ts:402` | `6e005b2` |

**Verification:**
- `npm run build` after each UI batch: `282→284kB ✓` (last `284.56kB`)
- Backend health `GET /health → {"status":"ok"}` (pid 12456, `Listen` on :8000), UI `vite ready on :3000` with `[orca-be] backend already running`
- MOSDAC data confirmed: `mdapi/mosdac_downloads` 5× 6.2 MB NetCDF `E06OCML4AC_*.nc` downloaded via `mdapi.py` (user: `farhan123`, boundingBox `64.5,4.6,95.5,24.0`, datasetId `E06OCML4AC`)

**Remaining for next session:** LLM `discussion+synthesis` fusion (`README.md:142`), verdict pill styling, optional Bhuvan/IMD/Twilio activation when creds exist. `AGENTS.md` / `ARCHITECTURE.md` pngs remain untracked.

**Commits pushed this session:** `422879d`, `68ab602`, `830b5d1`, `62e0d76`, `5642e22`, `c4fad4b`, `2edecfc`, `f0ad9f9`, `a1b139e`, `6e005b2` → `origin/main` `6e005b2` (plus follow-up `SESSION_SUMMARY` commit below).

---

## 13. Sessions 2026-08-28/29 — repo reconcile, INCOIS PFZ go-live, official answer format

**Context:** teammate rewrote `main` remotely (34 commits) with Firebase Auth
(Google sign-in UI), dashboard + latency rework, routing fixtures, fleet-convergence
demo and Docker/Render deployment. Local branch had 22 (mostly stale) commits the
remote didn't — so instead of force-pushing, the local INCOIS work was **re-applied
onto the rewritten `origin/main`** (cherry-pick reconcile, conflicts resolved
additively, junk-glue fixed, each commit verified compiling, then fast-forward
pushed). The old local lineage is preserved only in reflog.

### Deliverable 1 — INCOIS / SAMUDRA official PFZ is now the primary PFZ source

Official INCOIS feeds (keyless) replace the Bhuvan-derived/SST-front fallback as the
**default**; derived/simulated remain only as honest fallbacks. Getting credible
looking-feeds per SIH tier-3 expectations.

| Commit | What |
|---|---|
| `3a85744` | `data_connectors/incois_pfz.py` — pfzLines + pfzMobile + sector-text fetch, nearest-landing-centre advisory, DMS→decimal parse, 10-min TTL cache, `INCOIS_PFZ_TIMEOUT` env |
| `4e7d49d` | `pfz_agent.py` consumes official advisory (zone, landing centre, depth, validity); provider chain INCOIS_LIVE → DERIVED_LIVE (SST ring) → SIMULATED; `DataSource.INCOIS_LIVE` provenance |
| `b1f14dd` | `GET /api/pfz/live` endpoint + advisory context surfaced in answers |
| `27ef7cc` | UI: live PFZ map layer + landing-centre advisory card (source badge "Official INCOIS") |
| `5e1eeac` | README/ARCHITECTURE source table update + `PFZ_INCOIS_INTEGRATION.md` report |

**Verify (2026-08-29):** `GET /api/pfz/live` returns 300+ zones with landing centres /
issued advisories; Row-N-in answer as "safety certificate/coordinate prescription"
(per format requestor) — no more "simulated".

### Deliverable 2 — Official PFZ response now follows an EXACT format (no LLM drift)

| Commit | What |
|---|---|
| `cbf67a3` | `agents/pfz_output.py` (shared deterministic formatter) + `response_agent.py` and orchestrator fast path return the exact `🛡️ IMPORTANT / 🔶 VERDICT: … — … / 🎯 Target Coordinates / 📋 Quick Summary` template for official INCOIS PFZ lookups — 4-decimal coords, 1-decimal km, 8-point compass, `📡 Source: Official INCOIS Marine Fisheries (SAMUDRA) live advisory` |
| `3dd0c4a` | `pfz_agent.py` distance/bearing/target now computed to the **nearest point on the official digitized `pfzLines` geometry** (`_point_on_segment` + `_nearest_point_on_lines`, equirect + haversine, handles LineString/MultiLineString); removed "thermal-front heuristic / simulated chlorophyll / not an official advisory" wording from official path (`auto_router` reason reworded too) |
| `6f5619b` | `/viz/{session_id}` PFZ markers carry `pfz_lat/pfz_lon/distance_km_to_centre/distance_from_user_km/bearing_from_user_deg` |
| `dc6b9a3` | UI PFZ popup curated rows — Landing Centre, Distance from you, Bearing (+ compass word), Water depth, Coordinates, `🛡️ Official INCOIS PFZ`; `orcaApiService` skips its generic verdict callout when the answer already contains `🛡️ IMPORTANT` |

**Format guarantees enforced:** `format_pfz_answer()` returns `None` for any
non-INCOIS_LIVE source so derived/simulated answers keep honest wording; orchestrator
fast path short-circuits only when `intent == "pfz_lookup"` and fleet convergence did
not change the recommendation. Exact-template verification was run through
`ResponseAgent.run()` (E2E, no LLM) and printed byte-for-byte; nearest-point geometry
unit-checked (far line 441 km, local 0.2 km, empty → None). `py_compile` + `tsc` clean.

**Verification tooling used:** `python -m py_compile <changed files>`; unit probes via
`python -c` (UTF-8 console); `npx tsc --noEmit` exit 0 in `ORCA UI`.

### Repo / deploy state

- Local `main` == `origin/main` == `8e77b2a` (pushed 2026-08-30). Left untracked by design: `_pre_incois_backup/` snapshot, `architecture*.png`.
- Deployed backend at `https://orca-backend-1i5u.onrender.com` (Render free, Docker)
  can go cold; local fallback command (this session): `python -m uvicorn main:app --port 8000` — verified `/health` → `{"status":"ok"}` on `:8000`.
- Pythagoras note: `ORCA UI` vite dev auto-spawns the backend when `:8000` is free;
  Render pinned `CORS_ORIGINS` to the Firebase hosting origin.

---

## 14. Session 2026-08-30 — INCOIS consolidation master patch (8e77b2a)

**Context:** Full repo audit (every file/line) surfaced verified runtime crashes, conflicting marine providers (Open-Meteo/NOAA CoastWatch vs INCOIS), and unreadable single-line answers. One surgical pass fixed crashers first, then data-integrity, then UX — preserving FastAPI+LangGraph+React.

**Goal:** single `incois_marine.py` marine pipeline (Gemini PFZ + OSF SST/Wind + WW3 PHS01 swell + Currents + OceanSat-2:CHL ERDDAP), no fabricated values.

| Phase | Fix | Files |
|---|---|---|
| 1.1 | HazardAgent `None > float` guards (`wave_height_m`/`wind_gust_kmh` may be `None` on INCOIS live path) | `agents/hazard_agent.py:85` |
| 1.2 | Remove `_fetch_simulated_fallback` (nonexistent) → `_unavailable_reading` on ocean timeout | `orchestrator/__init__.py:1060` |
| 1.3 | `_serialize` dropping 7 dynamic fields → move `surface_current_mps`/`primary_swell_height_m`/`wind_direction*`/`marine_location_note`/`unavailable_reason`/`debug_incois` into `OceanStateReading` dataclass | `models.py:156`, `main.py:969` |
| 1.4 | `/query/voice` `async def` + sync `handle_query` blocking loop → `await asyncio.to_thread` | `main.py:272` |
| 2–3 | Delete `chlorophyll.py` (NOAA CoastWatch `coastwatch.pfeg.noaa.gov` / `nesdisNPP`); `incois_marine.py` already had `_fetch_chlorophyll` via `erddap.incois.gov.in` OceanSat-2:CHL; probe revealed `2011-02-02` archive only → honestly unavailable for 2026 | `data_connectors/chlorophyll.py` deleted, `data_connectors/incois_marine.py` |
| 2 | WMS `Value: 28.` truncated parse (INCOIS THREDDS `Value: 28.` without `SST` keyword) → explicit `Value:` fallback with `28.`→`28.0` | `data_connectors/incois_marine.py:95` |
| 2 | `trend_agent` + `bathymetry` + `geospatial` NOAA refs → INCOIS/GEBCO; `wind_divergence`/`models` comments | `agents/trend_agent.py`, `data_connectors/bathymetry.py` |
| 4 | Ensure `SST near me` → `ocean_state` only (Geospatial gated to `EEZ/boundary/route/…` intents, not blanket GPS) — already correct at `orchestrator/__init__.py:570` | verified |
| 6 | PFZ line style `#00E5FF` weight 4 opacity 1 + `[lon,lat]→[lat,lon]` + `fitBounds` | `ORCA UI/src/components/map/OceanMap.ts:34` |
| 7 | `marineService.ts` `resolveCoord` priority `mapPoint > GPS` | `ORCA UI/src/services/marineService.ts:39` |
| 8 | `hourly_series` flat `{times, wave_height_m, wind_gust_kmh}` + `viz_series` per-metric normalization | `agents/ocean_state_agent.py:220`, `main.py:621` |
| 9 | PFZ nondeterministic centre (`samples[0]` + `as_completed`) → coord match; `chlorophyll 0.0` fab → `None`; `chlorophyll_at_zone_mg_m3: float\|None` | `agents/pfz_agent.py:360`, `models.py:239` |
| 10 | Cyclone elsewhere forcing UNSAFE → only covering location/route forces UNSAFE; marine covering filtered to non-cyclone | `agents/hazard_agent.py:191` |
| 11 | Confidence counting `unavailable`/`tide_gauge_model` as live → exclude | `orchestrator/__init__.py:2051` |
| 12 | Readable answers: `"\n\n".join(parts)` + markdown table `| Parameter | Value |` with `📍` coords; `response_agent` LLM prompt + fallback both query-aware | `orchestrator/__init__.py:1646,1728`, `agents/response_agent.py:306` |
| 12 | Query-aware filtering: `sst` shows only SST, `wind` only wind, `wave` only waves, `chlorophyll` only chl, generic shows all | `orchestrator/__init__.py:1647`, `agents/response_agent.py:136` |
| 12 | Provenance `INCOIS OFFICIAL` fallback for marine keywords | `ORCA UI/src/components/chat/MessageItem.ts:264` |

**Verification 2026-08-30:**
- `py_compile` all `ORCA_Backend/**/*.py` → `ALL COMPILE OK`
- `import main, orchestrator, all agents, incois_marine, bathymetry` → `ALL IMPORTS OK`
- `test_optimized.py` → `All 21 tests passed`
- `ORCA UI` `vite build` → `✓ 67 modules transformed`
- Live probe `18.71,72.29` → after `Value: 28.` fix: `SST 28.29°C, Wind 31.76 km/h, Current 0.163 m/s, Swell 1.24 m, Chlorophyll unavailable (2011 archive)`; `sst at my location` now single-row SST table, `wind speed` single-row wind

**Remaining (unchanged from before, all credential/polish blocked):** Bhuvan optional,
`api.imd.gov.in` key + cyclone-cone overlays, Twilio live-fire, PostGIS/vector store,
LLM fusion for latency.

---

## 15. Session 2026-08-31 — MOSDAC chlorophyll (primary) + honest-data backlog sweep

**Context:** INCOIS ERDDAP OceanSat‑2:CHL is confirmed dead (archive `2011‑02‑02`, WMS `GetFeatureInfo` always empty for 2026). `isro_sources.MosdacConnector` was scaffolding only (`NotImplementedError`, placeholder `https://mosdac.gov.in/api`). Task: make MOSDAC the honest primary, keep INCOIS as free secondary, never simulate.

**What was done:**

| Area | Change | File:line |
|---|---|---|
| `MosdacConnector` | Real `fetch()` — `MOSDAC_API_KEY` (Bearer + `api_key` query), `MOSDAC_BASE_URL`/`MOSDAC_PRODUCT` env-overridable, JSON-first then text/CSV scan, `0-50 mg/m³` `_validate()`, `3‑day latency` note in returned dict; `build_request()` now returns real shape with `date=latest` | `data_connectors/isro_sources.py:47` |
| `OceanStateAgent` | Chlorophyll order `MOSDAC → INCOIS ERDDAP → unavailable` (`chl_source_detail` + `chl_latency_note`); `field_sources["chlorophyll_mg_m3"]` is `"live"` only on genuine success, otherwise `"unavailable"` (same pattern as SST/wind/current/swell); latency appended to `AgentTrace` + `reading.chlorophyll_latency_note` | `agents/ocean_state_agent.py:155` |
| `OceanStateReading` | Added `chlorophyll_latency_note: str | None` for provenance | `models.py:128` |
| Docs | `ORCA_Backend/README.md` Ocean-State row now `MOSDAC OCM primary (3‑day latency) → INCOIS secondary → unavailable`; root `README.md` agents table + status same; `isro_sources.py` docstring no longer says "Open‑Meteo is primary" | `ORCA_Backend/README.md:15`, `README.md:61` |
| Smoke test | `test_run.py:_smoke_chlorophyll()` — `Ratnagiri 17.0,73.3` asserts `0-50` + `live` or `None` + `unavailable`, never `simulated`, prints latency when present | `test_run.py:109` |
| Backlog docs | `isro_sources` now documents Registered General User includes OCM, Anonymous does NOT; `.env.example` already reserves `MOSDAC_API_KEY` (+ `MOSDAC_BASE_URL`/`MOSDAC_PRODUCT` overrides); `imd_live.py:60` `DISTRICT_IDS={}` still TODO via devtools, `geospatial_agent:15`/`models:293` `GEBCO TODO` untouched (real GEBCO wiring is a separate large task), Bhuvan optional, Twilio/PostGIS/LLM fusion remain polish/blocked | `data_connectors/isro_sources.py:1`, `.env.example:12`, `imd_live.py:60` |

**Verification:**
- `py_compile` all changed files → `ALL COMPILE OK`
- `test_run.py` smoke: `Ratnagiri 17.0,73.3 = unavailable [unavailable] ✓ (honest, no fab)` (MOSDAC no key + INCOIS dead → correctly `unavailable`, not `simulated`)
- `py_compile` `data_connectors/isro_sources.py`, `agents/ocean_state_agent.py`, `models.py` → `0`
- Existing suite (`test_optimized.py` etc.) still green — no regressions (INCOIS `500` for `20260830` is expected, not a code bug)

**Remaining (still externally blocked, now accurately documented):** `api.imd.gov.in` key, Bhoonidhi SAR creds, Twilio live-fire, GEBCO bathymetry for depth-aware routing, Bhuvan WMS optional, PostGIS/vector store, LLM fusion/TTS polish.
