# ---- Secrets (NEVER commit) ----
.env
.env.local
*.key
env.yaml
*-firebase-adminsdk-*.json
*service_account*.json
*service-account*.json

# ---- Python (ORCA_Backend) ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ---- Node (ORCA UI) ----
node_modules/
dist/
.firebase/
*.local

# ---- Logs / OS junk ----
*.log
.DS_Store
Thumbs.db

# ---- Local notes (never commit) ----
NOTES.md

# ---- Local backups (never commit) ----
_pre_incois_backup/
**/_pre_incois_backup/
*.zip

# ---- CI / Workflows (skip if lacking workflow scope) ----
.github/

# ---- Local smoke-test artifacts (never commit) ----
server_test.out
server_test.err
vite_test.out
vite_test.err
dashboard_test.json
dashboard_user.json

